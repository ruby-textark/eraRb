@checkOrgasmInTown
#dim dynamic orgasmThres
#dim dynamic orgasmCount
#dim dynamic orgasmMessage
#dim dynamic insuffOrgasm

	orgasmThres = (10000 - 19 * BASE:MASTER:혈중미약농도) * (1 + MARK:MASTER:절정봉인)
	for i, 501, 600
		if CFLAG:MASTER:i >= orgasmThres
			printFormW %STR:(i - 500)% 절정
			orgasmCount++
			DOWN:i = CFLAG:MASTER:i - CFLAG:MASTER:i % orgasmThres

			; 절정경험 증가
			EXP:MASTER:절정경험 += CFLAG:MASTER:i / orgasmThres
			EXP:MASTER:(i + 100) += CFLAG:MASTER:i / orgasmThres
		elseif CFLAG:MASTER:i >= 10000
			; 절정봉인 적용중
			insuffOrgasm++
		endif
	next

	if orgasmCount
		orgasmMessage = RAND:orgasmCount
		for i, 501, 600
		if CFLAG:MASTER:i >= orgasmThres
			if orgasmMessage == 0
				orgasmMessage -= 1
				if CFLAG:MASTER:i >= 2 * orgasmThres
					if RAND:5 == 0
						printFormW %kr(CALLNAME:MASTER, "은", "는")% 참다 못해 %bodyPartStr(i - 500)%의 쾌감으로 가버렸다
					elseif RAND:4 == 0
						printFormW 상상을 뛰어넘는 %bodyPartStr(i - 500)%의 쾌락에 %kr(CALLNAME:MASTER, "은", "는")% 눈을 까뒤집고 바들바들 떨었다
					elseif RAND:3 == 0
						printFormW %kr(CALLNAME:MASTER, "은", "는")% 척추를 관통하는 듯한 쾌감에 %bodyPartStr(i - 500)%를 마구 떨어대며 절정했다
					elseif RAND:2 == 0
						printFormW 한심한 울부짖음과 함꼐 %kr(CALLNAME:MASTER, "은", "는")% 몸을 가누지 못할 정도로 심하게 절정했다
					else
						printFormW 눈앞이 새하얘지는듯한 착각과 함께 %kr(CALLNAME:MASTER, "은", "는")% %bodyPartStr(i - 500)%로 크게 가버렸다
					endif
				else
					if RAND:5 == 0
						printFormW %kr(CALLNAME:MASTER, "은", "는")% %bodyPartStr(i - 500)%로 가버렸다
					elseif RAND:4 == 0
						printFormW %bodyPartStr(i - 500)%에서 주어지는 쾌락에 %kr(CALLNAME:MASTER, "은", "는")% 절정을 느꼈다
					elseif RAND:3 == 0
						printFormW %kr(CALLNAME:MASTER, "은", "는")% 숨을 헐떡이며 %bodyPartStr(i - 500)%에서 느껴지는 쾌락의 파도에 몸을 맡기고 있다
					elseif RAND:2 == 0
						printFormW %kr(CALLNAME:MASTER, "은", "는")% 꼴사납게 신음소리를 내며 %bodyPartStr(i - 500)%로 가버렸다
					else
						printFormW %kr(CALLNAME:MASTER, "은", "는")% 허리를 꺾으며 %bodyPartStr(i - 500)%의 쾌락에 굴복했다
					endif
				endif
			endif
		endif
	next
	elseif insuffOrgasm
		printFormL %kr(CALLNAME:MASTER, "은", "는")% 절정을 봉인당한 상태다
		
		if RAND:5 == 0
			printFormL %kr(CALLNAME:MASTER, "은", "는")% 온몸을 애처로울 정도로 떨고 있다
		elseif RAND:4 == 0
			printFormL %kr(CALLNAME:MASTER, "은", "는")% 간절한 눈빛으로 몸을 배배 꼬면서 %kr(CALLNAME:TARGET, "을", "를")% 바라보았다
		elseif RAND:3 == 0
			printFormL 이리저리 사지를 뒤틀어보지만, %kr(CALLNAME:MASTER, "이", "가")% 절정에 도달하는 일은 없었다
		elseif RAND:2 == 0
			printFormL 음침한 붉은 문신이 %CALLNAME:MASTER%의 몸에서 밝게 빛날 뿐, 파들파들 떨리는 그 몸에 절정은 허락되지 않았다
		else
			printFormL %kr(CALLNAME:MASTER, "은", "는")% 자존심도 내려놓고 스스로를 위로하려고 시도하지만, 효과가 있을 리가 없었다
		endif
	endif
