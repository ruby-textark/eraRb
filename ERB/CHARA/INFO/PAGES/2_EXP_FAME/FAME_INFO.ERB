@showFameInfo

	printL ‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥
	printL 명성에 대해
	if CFLAG:MASTER:기본명성 < 2000
		if RAND:2 == 0
			printFormL 아무도 %kr(CALLNAME:MASTER, "을", "를")% 모르고 있습니다.
		else
			printFormL %CALLNAME:MASTER%에게 신경쓸 여유가 있는 사람은 아무도 없습니다.
		endif
	elseif CFLAG:MASTER:기본명성 < 4000
		if RAND:2 == 0
			printFormL 종종 거리에서 %CALLNAME:MASTER%에게 말을 거는 사람들이 있습니다.
		else
			printFormL 거리에서 종종 익숙한 사람들이 %kr(CALLNAME:MASTER, "을", "를")% 힐끗거리며 처다봅니다.
		endif
	elseif CFLAG:MASTER:기본명성 < 6000
		if RAND:2 == 0
			printFormL %kr(CALLNAME:MASTER, "을", "를")% 보고 아는 체 하는 사람들이 꽤 있습니다.
		else
			printFormL 종종 거리에서 %CALLNAME:MASTER%에게 말을 거는 사람들이 있습니다.
		endif
	elseif CFLAG:MASTER:기본명성 < 8000
		if RAND:2 == 0
			printFormL 가끔 멀리에서 사람들이 %CALLNAME:MASTER%의 이름을 듣고 찾아오곤 합니다.
		else
			printFormL 사람들이 %kr(CALLNAME:MASTER, "을", "를")% 붙잡고 놓아주지 않을 정도입니다.
		endif 
	else
		if RAND:2 == 0
			printFormL %kr(CALLNAME:MASTER, "은", "는")% 모든 이들에게 환영받는 사람입니다.
		else
			printFormL 이 거리에서 %kr(CALLNAME:MASTER, "이", "가")% 모르는 사람은 없습니다.
		endif
	endif

	if CFLAG:MASTER:성적명성 < 2000
		if RAND:2 == 0
			printFormL %CALLNAME:MASTER%에게 흥미를 갖는 사람이 보이지 않습니다.
		else
			printFormL %CALLNAME:MASTER%에게는 어떤 눈길도 주어지지 않습니다.
		endif
	elseif CFLAG:MASTER:성적명성 < 4000
		if RAND:2 == 0
			printFormL 뒷골목에서 %CALLNAME:MASTER%의 이름이 들립니다.
		else
			printFormL 때때로 어둑한 거리에서 %kr(CALLNAME:MASTER, "을", "를")% 지켜보는 눈빛이 느껴집니다.
		endif
	elseif CFLAG:MASTER:성적명성 < 6000
		if RAND:2 == 0
			printFormL 대낮에도 %CALLNAME:MASTER%의 몸을 위아래로 훑어보는 시선이 느껴집니다.
		else
			printFormL 사람이 붐비는 곳에서 %CALLNAME:MASTER%의 몸을 더듬는 손이 생겼습니다.
		endif
	elseif CFLAG:MASTER:성적명성 < 8000
		if RAND:2 == 0
			printFormL 한밤중에 길을 걸을때면 뒤에서 발걸음 소리가 따라 들려옵니다.
		else
			printFormL %kr(CALLNAME:MASTER, "을", "를")% 담은 외설적인 그림이 길바닥에 돌아다니곤 합니다.
		endif 
	else
		if RAND:2 == 0
			printFormL 이제 뒷골목의 사람들은 %CALLNAME:MASTER%보다 %CALLNAME:MASTER%의 몸에 대해서 더 잘 알고 있습니다.
		else
			printFormL %CALLNAME:MASTER%에 대한 성적 시선을 바꾸기에는 이미 너무 늦었습니다.
		endif
	endif
