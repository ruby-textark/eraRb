@showAbilities
#dim dynamic ablIdx

	for ablIdx, 1, 1000
	
		; 신체감도는 별도의 탭으로 구분
		if 100 <= ablIdx && ablIdx <= 199
			continue
		endif
		
		if ABL:MASTER:ablIdx
			printForm %ABLNAME:ablIdx%+{ABL:MASTER:ablIdx} 
		endif
	next
	printL
