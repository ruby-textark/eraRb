; 화염병
@itemExpl3()
	returnform 던져서 사용 시 대상에게 100 피해를 준다.

@isUsable3(targetIdx)
#dim dynamic targetIdx

	return 1

@onUseItem3(targetIdx)
#dim dynamic targetIdx

	printFormW %CALLNAME:targetIdx%에게 %kr(ITEMNAME:2, "을", "를")% 사용했다.
	BASE:targetIdx:체력 = LIMIT(BASE:targetIdx:체력 - 100, 0, MAXBASE:targetIdx:체력)

	return 1

@isEquipable3
	return 0
@isUnequipable3
	return 0

@onEquip3
@onUnequip3
