; 화염병
@itemOption3()
	RESULTS '= "[대상피해+100]"
@itemExpl3()
	RESULTS '= "한 순간에 사그러들지라도, 피어난 불의 꽃에는 가시가 나 있어 닿은 이들에게 확실한 고통을 줄 것이다."

@isUsable3(targetIdx)
#dim dynamic targetIdx

	return 1

@onUseItem3(targetIdx)
#dim dynamic targetIdx

	printFormW %NAME:targetIdx%에게 %kr(ITEMNAME:2, "을", "를")% 사용했다.
	BASE:targetIdx:체력 = LIMIT(BASE:targetIdx:체력 - 100, 0, MAXBASE:targetIdx:체력)

	return 1

@isEquipable3
	return 0
@isUnequipable3
	return 0

@onEquip3
@onUnequip3
