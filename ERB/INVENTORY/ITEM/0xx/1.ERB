; 약초
@isUsable1(targetIdx)
#dim dynamic targetIdx

	return 1

@onUseItem1(targetIdx)
#dim dynamic targetIdx

	printFormW %CALLNAME:targetIdx%에게 %kr(ITEMNAME:1, "을", "를")% 사용했다.
	BASE:targetIdx:체력 = LIMIT(BASE:targetIdx:체력 + 50, 0, MAXBASE:targetIdx:체력)

	return 1

@isEquipable1()
	return 0
@isUnequipable1()
	return 0

@onEquip1()
@onUnequip1()
