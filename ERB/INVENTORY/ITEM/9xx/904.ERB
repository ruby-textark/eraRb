; 자궁확대약
@itemOption904()
	RESULTS '= "[자궁크기+100]"

@itemExpl904()
	RESULTS '= "사용시 자궁의 크기가 늘어난다."

@isUsable904(targetIdx)
#dim dynamic targetIdx

	return targetIdx == MASTER

@onUseItem904(targetIdx)
#dim dynamic targetIdx

	EXP:targetIdx:약물경험++
	BASE:targetIdx:자궁크기 = LIMIT(BASE:targetIdx:자궁크기 + 100, 100, 50000)
	
	return 1

@isEquipable904
	return 0
@isUnequipable904
	return 0

@onEquip904
@onUnequip904
