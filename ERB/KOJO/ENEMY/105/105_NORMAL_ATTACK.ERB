@c105onNormalAttack

	if RAND:5 && BASE:MASTER:체력 < MAXBASE:MASTER:체력 / 4
		call c105onSexualAttack
	elseif RAND:3 && BASE:MASTER:체력 < MAXBASE:MASTER:체력 / 4
		call c105onSexualAttack
	else
		call fallbackOnNormalAttack_e
	endif
