@onSexualHarassment_m(partIdx)
#dim dynamic partIdx

	if !CFLAG:MASTER:의식있음
		return
	endif

	; todo 여기 채워야됨
	if partIdx == 102
		if RAND:3 == 0
			printFormW %kr(CALLNAME:MASTER, "은", "는")% 목을 졸린 채 좋을대로 입술을 희롱당했다.
			printFormW 핵핵대며 숨을 거칠게 몰아쉬는 %CALLNAME:MASTER%의 몸은 목숨을 위협받는 상황에서조차 쾌락을 찾고 있는 것 같다.
			EXP:MASTER:질식경험++
		else
			printFormW 갑작스레 팔을 붙들린 채 입술로 밀고 들어오는 혀의 감각에 %kr(CALLNAME:MASTER, "은", "는")% 불쾌함과 동시에 알 수 없는 뜨거움이 느껴졌다.
		endif
	elseif partIdx == 112
		if RAND:3 == 0
			printFormW 강하게 %kr(partStr(112), "을", "를")% 쥐어지자 %kr(CALLNAME:MASTER, "은", "는")% 고통에 표정을 확 찌푸렸다.
			EXP:MASTER:피학경험++
		else
			printFormW %kr(CALLNAME:MASTER, "은", "는")% 가벼운 터치로 %kr(partStr(112), "을", "를")% 애무당해 몸을 부르르 떨었다.
		endif
	elseif partIdx == 113
		if RAND:3 == 0
			printFormW 옷 속으로 들어온 손이 %kr(partStr(113), "을", "를")% 톡톡 건드리며 약올리자 %kr(CALLNAME:MASTER, "은", "는")% 야릇한 기분에 몸이 달아올랐다.
		else
			if PALAM:MASTER:113 > 2000 || PALAM:MASTER:113 > masterOrgasmThres() / 2
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 옷 너머로 솟아오른 %kr(partStrRaw(113), "을", "를")% 자극당해 무심결에 기대하게 되어 버린다.
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 옷 너머로 %partStrRaw(113)% 주변을 살살 문지르며 애태워지는 바람에 그만 달콤한 목소리를 내고 말았다.
			endif
		endif
	elseif partIdx == 121
		if RAND:3 == 0
			printFormW 불쑥 다가온 손이 %CALLNAME:MASTER%의 %partStr(121)% 안으로 손가락을 밀어넣었다.
		else
			if PALAM:MASTER:121 > 2000 || PALAM:MASTER:121 > masterOrgasmThres() / 2
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 이미 한껏 기대중인 %kr(partStrRaw(121), "을", "를")% 휘젓는 손가락에 허리를 움찔거리고 있다.
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% %kr(partStrRaw(121), "을", "를")% 파고드는 손짓에 심장이 두근거리는 것을 느꼈다.
			endif
		endif
	elseif partIdx == 122
		if RAND:3 == 0
			printFormW 아래쪽으로 향한 손이 %kr(partStr(121), "을", "를")% 살짝 꼬집으며 자극하는 바람에 %kr(CALLNAME:MASTER, "은", "는")% 온몸에 소름이 돋는 느낌이 들었다.
			EXP:MASTER:피학경험++
		else
			if PALAM:MASTER:122 > 2000 || PALAM:MASTER:122 > masterOrgasmThres() / 2
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 옷 너머로 솟아오른 %kr(partStrRaw(113), "을", "를")% 자극당해 무심결에 기대하게 되어 버린다.
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 옷 너머로 %partStrRaw(113)% 주변을 살살 문지르며 애태워지는 바람에 그만 달콤한 목소리를 내고 말았다.
			endif
		endif
	elseif partIdx == 123
		if RAND:3 == 0
			printFormW 불쑥 다가온 손이 %CALLNAME:MASTER%의 %partStr(123)% 근처를 살살 간지럽히며 약올려왔다.
			printFormW 더러운 구멍에 대한 말초적인 음심이 %CALLNAME:MASTER%에게 배덕적인 쾌락을 불러와 한순간 진심으로 애태움은 그만두고 넣어주길 바라게 되었다.
		else
			if PALAM:MASTER:123 > 2000 || PALAM:MASTER:123 > masterOrgasmThres() / 2
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 이미 한껏 기대중인 %kr(partStrRaw(123), "을", "를")% 휘젓는 손가락에 허리를 움찔거리고 있다.
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% %kr(partStrRaw(123), "을", "를")% 살짝살짝 벌리려고 하는 손가락에 심장이 두근거리는 것을 느꼈다.
			endif
		endif
	endif

	printFormW 정신을 차린 %kr(CALLNAME:MASTER, "이", "가")% 더는 안되겠다 싶어 강하게 제지하려 하자, %kr(CALLNAME:TARGET, "은", "는")% 어느새 저 멀리 도망가고 있었다.
