@onEnemyCum_m(partIdx)
#dim dynamic partIdx

	if !CFLAG:MASTER:의식있음
		return
	endif

	if ABL:MASTER:흡정
		printFormL %kr(CALLNAME:TARGET, "은", "는")% %CALLNAME:MASTER%에게 착정당하고 있다

		; 흡정 1레벨당 최대 마력의 1%를 강탈
		if BASE:TARGET:마력 >= MAXBASE:TARGET:마력 * MIN(ABL:MASTER:흡정, 5) / 100
			DOWNBASE:TARGET:마력 += MAXBASE:TARGET:마력 * MIN(ABL:MASTER:흡정, 5) / 100
		; 무족하면 패널티
		; 흡정 1레벨당 최대 체력의 0.25%
		else
			printFormL 하지만 %CALLNAME:TARGET%의 마력은 이미 바닥이었고, 대신해서 체력을 흡수당했다

			DOWNBASE:TARGET:마력 = BASE:TARGET:마력
			DOWNBASE:TARGET:체력 = MAXBASE:TARGET:체력 * MIN(ABL:MASTER:흡정, 5) / 400
		endif
	endif

	if isMasterLewd() && isMasterSubmissive()
		if isInsertablePart(partIdx)
			if RAND:3 == 0
				printFormW 몸을 흔들며 %kr(CALLNAME:MASTER, "은", "는")% 정액이 구멍을 가득 채우는 감각을 즐겼다
			elseif RAND:2 == 0
				printFormW 차분히 손을 얹고 %kr(CALLNAME:MASTER, "은", "는")% 구멍 안으로 쏟아져 들어오는 정액을 피부 너머로 즐겼다
			else
				printFormW 열이 오른 %kr(CALLNAME:MASTER, "은", "는")% 무의식중에 구멍을 좁히며 자지님을 기쁘게 하고 있다
			endif
		else
			if RAND:3 == 0
				printFormW 음란한 미소를 지으며 %kr(CALLNAME:MASTER, "은", "는")% 뜨거운 정액을 피부으로 음미했다
			elseif RAND:2 == 0
				printFormW 황홀한 눈빛으로 %kr(CALLNAME:MASTER, "은", "는")% 흩뿌려진 정액이 너무 아쉽다는 표정을 짓고 있다
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 흘러내리는 정액을 손으로 모아 핱는 것으로도 모자라 손가락을 빨고 있다
			endif

		endif
	elseif isMasterLewd() && !isMasterSubmissive()
		if isInsertablePart(partIdx)
			if RAND:3 == 0
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 이를 악물고 반항하지만 구멍 안에 따라지는 정액에 몸이 저절로 반응하는 것을 참을 수는 없었다
			elseif RAND:2 == 0
				printFormW 한껏 싫은 표정을 지은 %kr(CALLNAME:MASTER, "이지만", "지만")% 사정당하고 나자 표정이 풀어지는 것은 감출 수 없었다
			else
				printFormW 겉으로는 반항하는 것 같지만 %CALLNAME:MASTER%의 안은 꽉 조이며 더욱 정액을 달라 보채고 있었다
			endif
		else
			if RAND:3 == 0
				printFormW 아닌 척 화난 눈길을 보내지만 %kr(CALLNAME:MASTER, "은", "는")% 몸에 토해진 정액에 흘깃 시선이 가는 듯 하다
			elseif RAND:2 == 0
				printFormW 황홀한 %kr(CALLNAME:MASTER, "은", "는")% 되도 않는 반항을 시도해 보지만 쏟아지는 정액에 발정한 나머지 실패하고 말았다
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 눈을 한껏 찌푸리지만 아랑곳않고 뿌려지는 정액에 몸을 움찔거렸다
			endif

		endif
	elseif !isMasterLewd() && isMasterSubmissive()
		if isInsertablePart(partIdx)
			if RAND:3 == 0
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 딱히 거부하지는 않았지만 사정당한 것에 대해 이질감을 느끼고 있는 것 같다
			elseif RAND:2 == 0
				printFormW 안쪽에서부터 느껴지는 뜨거운 감각에 %kr(CALLNAME:MASTER, "은", "는")% 몸을 잠깐 움찔했다
			else
				printFormW 일단은 받아들이는 %kr(CALLNAME:MASTER, "이었지만", "였지만")% 정액의 감각이 썩 내키지는 않는 것 같다
			endif
		else
			if RAND:3 == 0
				printFormW 그럭저럭 순응하고는 있지만 %kr(CALLNAME:MASTER, "이", "가")% 몸에 묻은 정액에 익숙해지려면 아직은 시간이 걸릴 것 같다
			elseif RAND:2 == 0
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 사정의 순간 잠시 눈을 깜빡였지만 그렇다고 딱히 정액을 꺼리지는 않는 모양이다
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 몸을 움찔하긴 했지만 흩뿌려지는 정액을 피하지 않고 그대로 몸으로 받아냈다
			endif

		endif
	elseif !isMasterLewd() && !isMasterSubmissive()
		if isInsertablePart(partIdx)
			if RAND:3 == 0
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 몸 안에 정액이 부어지는 것에 참을 수 없는 치욕을 느끼는 듯하다
			elseif RAND:2 == 0
				printFormW 잔뜩 찌푸린 채 몸의 구멍으로 묵묵히 사정을 받아낸 %kr(CALLNAME:MASTER, "은", "는")% 역겨움을 감추려고 하지도 않고 있다
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 사정당한 순간 몸을 떨며 수치를 간신히 견뎠다
			endif
		else
			if RAND:3 == 0
				printFormW 부들거리며 분을 삭이는 %kr(CALLNAME:MASTER, "은", "는")% 정액이 뿌려지자 순간 덤벼들려 했지만 별로 좋은 생각이 아니라는걸 깨닫고 간신히 멈췄다
			elseif RAND:2 == 0
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 몸을 틀어 뿌려지는 정액을 피하려 했지만 역부족이었다
			else
				printFormW %kr(CALLNAME:MASTER, "은", "는")% 원하지도 않는 정액이 몸에 쏟아지는 것에 극도로 반발하고 있다
			endif

		endif
	endif

	if MARK:MASTER:음문각인
		printFormW %CALLNAME:MASTER%의 하복부에 새겨진 음문이 정액을 만나 기뻐하고 있다
	endif
