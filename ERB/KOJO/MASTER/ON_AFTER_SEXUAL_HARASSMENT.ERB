@onAfterSexualHarassment_m(partIdx)
#dim dynamic partIdx

	if !CFLAG:MASTER:의식있음
		return
	endif
	
	printFormW SH {partIdx}

	; todo 여기 채워야됨
	if partIdx == 102
		if RAND:5 == 0
		elseif RAND:4 == 0
		elseif RAND:3 == 0
		elseif RAND:2 == 0
		else
		endif
	elseif partIdx == 112
	elseif partIdx == 113
	elseif partIdx == 121
	elseif partIdx == 122
	elseif partIdx == 123
	endif
