@onMasterInserted_m(partIdx, insertType)
#dim dynamic partIdx
#dim dynamic insertType

	if !CFLAG:MASTER:의식있음
		return
	endif
	
	; todo 이하 작성할것
	
	if TALENT:MASTER:이종간기호 && CFLAG:TARGET:종족 != 1
	endif
	
	if TALENT:MASTER:강간선호 && !FLAG:성교합의
	endif
