@EVENTTURNEND

	printFormL 하루가 끝났습니다
	
	printFormW %kr(CALLNAME:MASTER, "은", "는")% 푹 쉬었다
	BASE:MASTER:체력 = LIMIT(BASE:MASTER:체력 + MAXBASE:MASTER:체력 / 5, 0, MAXBASE:MASTER:체력)
	BASE:MASTER:마력 = LIMIT(BASE:MASTER:마력 + MAXBASE:MASTER:마력 / 10, 0, MAXBASE:MASTER:마력)

	DAY += 1
	
	if DAY % 7 == 0
		printFormW 1주일이 지나 숙박비 2000$가 차감됩니다
		MONEY -= 2000
	endif
	
	call parasite
	call mensAndPreg
	call holeTight
	call addiction
	call tattoo
	call arrest

	begin shop
