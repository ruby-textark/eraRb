; 기본적으로는 @USERSHOP 종료 후에 호출
; 하지만 전투시 train 종료 후에도 호출됨
@afterAction

	call clearFlags
	call applyUpDown

	call arrest
	call hypnoEvent
	call cumDump
	call ablCheck
	call talentCheck
	call checkOrgasm
