@USERSHOP
#dim dynamic menuResult

	if FLAG:원정중
		call continueExpeditionHandleResult
		return
	endif

	call processMenu
	menuResult = RESULT
	
	if menuResult
		call afterAction
	endif

	return RESULT
