@USERSHOP
#dim dynamic menuResult

	if FLAG:원정중
		call continueExpeditionHandleResult

		if RESULT
			return
		endif
	endif

	call processMenu
	menuResult = RESULT
	
	if menuResult
		call afterAction
	endif

	return RESULT
