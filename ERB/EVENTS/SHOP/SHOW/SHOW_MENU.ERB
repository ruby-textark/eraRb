@showMenu
	; Target does not exist
	if !isMasterFree() || TARGET == 0
		setcolor 0x777777
	; 자궁대여중
	elseif FLAG:자궁대여료
		setcolor 0x777777
	endif
	printLC [101] - 출정
	resetcolor

	; Target already exists
	if !isMasterFree() || TARGET == 1
		setcolor 0x777777
	endif
	printLC [102] - 의뢰 수주
	resetcolor

	; Target does not exist
	if !isMasterFree() || TARGET == 0
		setcolor 0x777777
	endif
	printLC [103] - 의뢰 취소
	resetcolor
	printL

	printLC [201] - 상태창
	printLC [202] - 인벤토리
	if !isMasterFree()
		setColor 0x777777
	endif
	printLC [203] - 장비변경
	resetColor
	printL

	if !isMasterFree()
		setColor 0x777777
	endif
	printLC [301] - 아이템 거래
	printLC [302] - 뒷골목
	resetColor
	printL
	
	printL

	printLC [801] - 저장
	printLC [802] - 불러오기
	printL
	
	printLC [900] - 휴식

	;printLC [103] - 캐릭터추가
