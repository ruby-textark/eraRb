@backStreet

	printFormW %kr(CALLNAME:MASTER, "은", "는")% 뒷골목에 발을 들였다

$inputLoop
	
	clearLine 99999
	if RAND:2 == 0
		call backStreetSexualHarassment
	else
		call backStreetSexualFame
	endif

	printL 어디로 갈까
	printFormL [1] - 암거래상
	printFormL [2] - 홍등가
	printFormL [3] - 신체거래
	printFormL [0] - 돌아가기
	input

	if RESULT == 1
		call itemShopMenu(900, 1000)
	elseif RESULT == 2
		call brothel
	elseif RESULT == 3
		call slavery
	elseif RESULT == 0
		return
	else
		call systemError("잘못된 입력입니다")
		clearLine 2
	endif

	goto inputLoop
