@expedition
#dim dynamic randValue

	randValue = frand(100)

	if randValue < 60
		; 전투(60%)
		call startBattle
	elseif randValue < 80
		; 이로운방(20%)
		call goodRoom
	else
		; 해로운방(20%)
		call badRoom
	endif

@startBattle

	call addMonster(FLAG:미궁위치 * 100 + 1 + RAND:(FLAG:(100 + FLAG:미궁위치 * 10)))
	
	printFormW %CALLNAME:TARGET% %kr(NAME:TARGET, "과", "와")% 마주쳤습니다. 전투를 개시합니다.
	printL
	
	begin train

	return

@goodRoom
#dim dynamic randValue


	; todo 여기 작업
	randValue = frand(100)
	
	if randValue < 33
		; 상점(33%)
	elseif randValue < 66
		; 샘(33%)
	else
		; 보물상자(33%)
	endif

@badRoom
	; todo 여기 작업
	call startBattle
	
@bossBattle

	;
	printW 알 수 없는 검은 기운이 앞을 가로막고 있다.
	printW 더 이상 진행할 수는 없을 것 같다.
