@startQuest
#dim dynamic partIdx

	printFormL %kr(CALLNAME:TARGET, "을", "를")% 토벌하기 위해 모험을 떠납니다.

	; Some misc events come here.

	; PALAM -> CFLAG 인계
	for partIdx, 501, 600
		CFLAG:MASTER:partIdx = PALAM:MASTER:(partIdx - 400)
	next

	begin train
