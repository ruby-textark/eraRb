@cancelQuest
#dim dynamic decreasedFame

	FLAG:퀘스트진행중 = 0
	printFormW 퀘스트를 취소합니다

	if CFLAG:1:캐릭터번호 != 1001
		call delMonster
	endif
	
	decreasedFame = RAND:5
	
	printFormW 명성이 {decreasedFame} 감소했다.

	if CFLAG:MASTER:기본명성 < 2000
		printFormW 게다가 실력도 없는 것이 분수도 모른다며 악명이 쌓이고 말았다.
		CFLAG:MASTER:악명 = LIMIT(CFLAG:MASTER:악명 + 5 + RAND:5, 0, 10000)
	endif

	CFLAG:MASTER:기본명성 = LIMIT(CFLAG:MASTER:기본명성 - decreasedFame, 0, 10000)

	return 1
