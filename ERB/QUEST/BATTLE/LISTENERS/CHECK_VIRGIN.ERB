@checkVirgin

	for i, 301, 399
		if CFLAG:MASTER:i && TALENT:MASTER:i == 1
			printFormL %kr(CALLNAME:MASTER, "은", "는")% 처음으로 %bodyPartStr(i - 200)%로 하는 성교를 경험했다.
			TALENT:MASTER:i = 0
		endif
	next
