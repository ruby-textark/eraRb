@applyBuffTo(charIdx, buffIdx, buffTurn)
#dim dynamic charIdx
#dim dynamic buffIdx
#dim dynamic buffTurn

	TCVAR:charIdx:buffIdx += buffTurn
	callForm onBuff{buffIdx}(charIdx)

@hasBuff(charIdx, buffIdx)
#function
#dim dynamic charIdx
#dim dynamic buffIdx

	returnf TCVAR:charIdx:buffIdx > 0
