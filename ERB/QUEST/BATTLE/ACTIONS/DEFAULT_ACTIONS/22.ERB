@COM_ABLE22

	return !isMasterDown() && isTargetInRange()

@COM22

	printL 급소를 노렸다!
	
	if !checkAmmo() && !isSameLocation()
		printFormL 하지만 %kr(getAmmoName(), "이", "가")% 부족하여 실패했다...
		return 0
	endif

	if RAND:10 < 6
		printL 빗나갔다!
		return 1
	endif
	
	LOSEBASE:체력 = randomizeDamage(10 * MAX(1, getActualAtk(MASTER) - getActualDef(TARGET) / 2))
	
	call useAmmo()

	return 1
