@COM_ABLE21

	return !isMasterDown() && isTargetInRange()

@COM21

	printL 적을 공격한다
	
	if !checkAmmo() && !isSameLocation()
		printFormL 하지만 %kr(getAmmoName(), "이", "가")% 부족하여 실패했다...
		return 0
	endif

	if RAND:10 < 1
		printL 빗나갔다!
		return 1
	endif
	
	LOSEBASE:체력 = randomizeDamage(10 * MAX(1, getActualAtk(MASTER) - getActualDef(TARGET)))
	
	call useAmmo()

	return 1
