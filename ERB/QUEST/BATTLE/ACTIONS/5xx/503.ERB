@COM_ABLE503

	; 음마의반지 장착시 최대 인접 1칸까지 대상으로 사용가능
	return EQUIP:MASTER:장신구 == 1702 && inBattleDistance() <= 1 

@COM503
#dim dynamic damageBase

	printW 죽음의 키스를 날렸다
	
	if BASE:MASTER:마력 < 24
		printFormW 하지만 마력이 부족하여 실패했다...
		return 0
	endif
	BASE:MASTER:마력 -= 24

	; 기본 마법 공격계수의 1.2배 적용 및 욕망 보정 추가
	damageBase = 24 * MAX(2, getActualMatk(MASTER) * (ABL:MASTER:욕망 + 3) - getActualMdef(TARGET))
	LOSEBASE:체력 = damageBase * 9 / 10 + RAND:(damageBase / 10)

	; 반작용으로 자궁쾌감상승
	SOURCE:MASTER:욕정 += 100
	SOURCE:MASTER:쾌자궁 += 100

	return 1
