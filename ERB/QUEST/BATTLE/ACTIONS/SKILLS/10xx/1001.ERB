@COM_ABLE1001

	; 나무지팡이 장착시 사용가능
	return isSkillAvailable(1001, "무기", 1605)

@COM1001
#dim dynamic damageBase

	printL 마법 화살을 발사했다!
	
	if BASE:MASTER:마력 < 40
		printL 하지만 마력이 부족하여 실패했다...
		return 0
	endif
	BASE:MASTER:마력 -= 40
	
	damageBase = 20 * MAX(2, getActualMatk(MASTER) - getActualMdef(TARGET))
	LOSEBASE:체력 = damageBase * 9 / 10 + RAND:(damageBase / 10)	

	return 1
